<form method="post" action="{{ path('mota_delete', {'id': motum.id}) }}" >
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ motum.id) }}">
    <button class="btnDeleteButton btn btn-xs btn-danger"><i class="fas fa-trash-alt"></i></button>
</form>
