<form method="post" action="{{ path('tipo_iva_delete', {'id': tipo_iva.id}) }}">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tipo_iva.id) }}">
    <button class="btnDeleteButton btn btn-xs btn-danger"><i class="fas fa-trash-alt"></i></button>
</form>
