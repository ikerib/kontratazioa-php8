<form method="post" action="{{ path('kontratista_delete', {'id': kontratistum.id}) }}" >
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ kontratistum.id) }}">
    <button class="btnDeleteButton btn btn-xs btn-danger"><i class="fas fa-trash-alt"></i></button>
</form>
