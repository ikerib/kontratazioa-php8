{% extends 'base.html.twig' %}

{% block title %}Fitxategia{% endblock %}

{% block body %}
    <h1>Fitxategia</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ fitxategium.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ fitxategium.name }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ fitxategium.createdAt ? fitxategium.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ fitxategium.updatedAt ? fitxategium.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('fitxategia_index') }}">back to list</a>

    <a href="{{ path('fitxategia_edit', {'id': fitxategium.id}) }}">edit</a>

    {{ include('fitxategia/_delete_form.html.twig') }}
{% endblock %}
