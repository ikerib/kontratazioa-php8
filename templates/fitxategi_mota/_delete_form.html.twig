<form method="post" action="{{ path('fitxategi_mota_delete', {'id': fm.id}) }}">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ fm.id) }}">
    <button class="btnDeleteButton btn btn-xs btn-danger"><i class="fas fa-trash-alt"></i></button>
</form>
