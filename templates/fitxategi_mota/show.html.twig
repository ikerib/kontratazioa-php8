{% extends 'base.html.twig' %}

{% block title %}FitxategiMota{% endblock %}

{% block body %}
    <h1>FitxategiMota</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ fitxategi_motum.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ fitxategi_motum.name }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ fitxategi_motum.createdAt ? fitxategi_motum.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ fitxategi_motum.updatedAt ? fitxategi_motum.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('fitxategi_mota_index') }}">back to list</a>

    <a href="{{ path('fitxategi_mota_edit', {'id': fitxategi_motum.id}) }}">edit</a>

    {{ include('fitxategi_mota/_delete_form.html.twig') }}
{% endblock %}
