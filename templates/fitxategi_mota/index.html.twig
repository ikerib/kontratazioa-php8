{% extends 'base.html.twig' %}

{% block title %}Motak{% endblock %}

{% block breadcrumb %}
    <li class="breadcrumb-item active">Fitxategi motak</li>
{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-12">
            <div class="card">
                <div class="card-header">
                    <h3 class="card-title">Sailak</h3>
                    <div class="card-tools">
                        <a class="btn btn-outline-primary" href="{{ path('fitxategi_mota_new') }}">Fitxategi mota berria</a>
                    </div>
                </div>
                <div class="card-body">
                    <table id="myDatatable" class="table table-bordered table-striped table-sm">
                        <thead>
                        <tr>
                            <th>#</th>
                            <th>{{ "Izena" | trans}}</th>
                            <th class="text-center">{{ "Actions" | trans }}</th>
                        </tr>
                        </thead>
                        <tbody>
                        {% for fm in fitxategi_motak %}
                            <tr>
                                <td>{{ fm.id }}</td>
                                <td>{{ fm.name }}</td>
                                <td class="text-center">
                                    <ul class="list-inline">
                                        <li class="list-inline-item">
                                            <a class="btn btn-xs btn-primary" href="{{ path('fitxategi_mota_edit', {'id': fm.id}) }}"><i class="fas fa-edit"></i></a>
                                        </li>
                                        <li class="list-inline-item">
                                            {{ include('fitxategi_mota/_delete_form.html.twig') }}
                                        </li>
                                    </ul>
                                </td>
                            </tr>
                        {% else %}
                            <tr>
                                <td colspan="3">no records found</td>
                            </tr>
                        {% endfor %}
                        </tbody>
                    </table>

                </div>
                <div class="card-footer"></div>

            </div>
        </div>
    </div>
{% endblock %}
