<form method="post" action="{{ path('prozedura_delete', {'id': prozedura.id}) }}" >
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ prozedura.id) }}">
    <button class="btnDeleteButton btn btn-xs btn-danger"><i class="fas fa-trash-alt"></i></button>
</form>
