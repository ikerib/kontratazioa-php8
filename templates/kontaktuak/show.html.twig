{% extends 'base.html.twig' %}

{% block title %}Kontaktuak{% endblock %}

{% block body %}
    <h1>Kontaktuak</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ kontaktuak.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ kontaktuak.name }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ kontaktuak.email }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('kontaktuak_index') }}">back to list</a>

    <a href="{{ path('kontaktuak_edit', {'id': kontaktuak.id}) }}">edit</a>

    {{ include('kontaktuak/_delete_form.html.twig') }}
{% endblock %}
