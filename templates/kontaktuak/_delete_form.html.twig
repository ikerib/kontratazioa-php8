<form method="post" action="{{ path('kontaktuak_delete', {'id': kontaktua.id}) }}">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ kontaktua.id) }}">
    <button class="btnDeleteButton btn btn-xs btn-danger"><i class="fas fa-trash-alt"></i></button>
</form>
