{% extends 'base.html.twig' %}

{% block title %}Egoera{% endblock %}

{% block body %}
    <h1>Egoera</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ egoera.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ egoera.name }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ egoera.createdAt ? egoera.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ egoera.updatedAt ? egoera.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('egoera_index') }}">back to list</a>

    <a href="{{ path('egoera_edit', {'id': egoera.id}) }}">edit</a>

    {{ include('egoera/_delete_form.html.twig') }}
{% endblock %}
