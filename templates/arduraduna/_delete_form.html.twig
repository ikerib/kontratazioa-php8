<form method="post" action="{{ path('arduraduna_delete', {'id': arduraduna.id}) }}">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ arduraduna.id) }}">
    <button class="btnDeleteButton btn btn-xs btn-danger"><i class="fas fa-trash-alt"></i></button>
</form>
