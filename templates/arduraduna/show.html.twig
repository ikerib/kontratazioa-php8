{% extends 'base.html.twig' %}

{% block title %}Arduraduna{% endblock %}

{% block body %}
    <h1>Arduraduna</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ arduraduna.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ arduraduna.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('arduraduna_index') }}">back to list</a>

    <a href="{{ path('arduraduna_edit', {'id': arduraduna.id}) }}">edit</a>

    {{ include('arduraduna/_delete_form.html.twig') }}
{% endblock %}
